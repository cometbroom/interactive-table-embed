# Locally Interactive Table

A simple, beautiful, and fully functional editable table that can be embedded.

**Features:**
- Edit cells directly in the table
- Add / delete rows
- Data automatically saved in the visitor's browser (`localStorage`)
- No backend or login required
- Export to CSV
- Single HTML file